// data/config/BuildConfig.kt
package com.example.gigwork.data.config

object BuildConfig {
    // These will be populated from gradle during build time
    const val SUPABASE_URL = BuildConfig.SUPABASE_URL
    const val SUPABASE_KEY = BuildConfig.SUPABASE_KEY
}